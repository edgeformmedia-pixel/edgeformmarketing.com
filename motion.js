const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,progress=document.querySelector('.scroll-progress'),header=document.querySelector('header');
const updateScroll=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max>0?scrollY/max*100:0}%`;header.classList.toggle('scrolled',scrollY>40);if(!reduced)document.querySelectorAll('[data-parallax]').forEach(el=>{const r=el.getBoundingClientRect();el.style.transform=`translate3d(0,${(r.top-innerHeight/2)*-.035}px,0)`})};
document.querySelectorAll('.statement h2,.statement>div,.capabilities>h2,.model>div,.sectors>div,.insights>h2,.contact>*').forEach((el,i)=>{el.classList.add('motion-reveal');if(i%3===1)el.classList.add('from-left')});document.querySelectorAll('.cards,.notes,.metrics').forEach(el=>el.classList.add('motion-stagger'));document.querySelectorAll('.model figure,.orbit').forEach(el=>el.classList.add('motion-reveal','from-right'));document.querySelector('.band img')?.setAttribute('data-parallax','');
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;entry.target.classList.add('is-visible');entry.target.querySelectorAll?.('[data-count]').forEach(counter=>{const target=Number(counter.dataset.count),started=performance.now();const tick=now=>{const p=Math.min(1,(now-started)/950);counter.textContent=Math.round(target*(1-Math.pow(1-p,3)));if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)});observer.unobserve(entry.target)}),{threshold:.16,rootMargin:'0px 0px -8% 0px'});document.querySelectorAll('.motion-reveal,.motion-stagger,.metrics').forEach(el=>observer.observe(el));addEventListener('scroll',updateScroll,{passive:true});updateScroll();
